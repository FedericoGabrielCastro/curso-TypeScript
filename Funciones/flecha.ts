// funciones de flecha ( arrow functions )
// Notacion de tipo.

const sumar = (valor1: number, valor2: number) : number => {  // :number al final inidica que siempre va a ser un tipo number en el return
    return valor1 + valor2 
}